apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mrrobot-api.fullname" . }}-test-connection"
  labels:
    {{- include "mrrobot-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mrrobot-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
