apiVersion: v1
kind: Service
metadata:
  labels:
    app: mr-robot
  name: mr-robot-api-service
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app: mr-robot
  type: LoadBalancer
